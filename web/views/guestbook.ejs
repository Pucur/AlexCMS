<%/*
#############################################################
#     ▄▄    ▄▄             ▄   ▄▄▄▄  ▄▄▄     ▄▄▄    ▄▄▄▄▄   #
#   ▄█▀▀█▄   ██            ▀██████▀   ███▄ ▄███    ██▀▀▀▀█▄ #
#   ██  ██   ██              ██       ██ ▀█▀ ██    ▀██▄  ▄▀ #
#   ██▀▀██   ██ ▄█▀█▄▀██ ██▀ ██       ██     ██      ▀██▄▄  #
# ▄ ██  ██   ██ ██▄█▀  ███   ██       ██     ██    ▄   ▀██▄ #
# ▀██▀  ▀█▄█▄██▄▀█▄▄▄▄██ ██▄ ▀█████ ▀██▀     ▀██▄  ▀██████▀ #
#############################################################
*/%>
<body>

<h1><%= pageMenu.name %></h1>

<form method="POST">

  <input 
    name="name" 
    placeholder="<%= lang === 'en' ? 'name' : 'név' %>">

  <br>

  <textarea 
    name="message" 
    placeholder="<%= lang === 'en' ? 'message' : 'üzenet' %>"></textarea>

  <br>

  <button>
    <%= lang === 'en' ? 'Send' : 'Küldés' %>
  </button>

</form>

<hr>

<% if (messages && messages.length) { %>

  <% messages.forEach(m => { %>

    <p>
      <b><%= m.name %></b>: <%= m.message %>

      <% if (user && user.role === 'admin') { %>
        <form method="POST" action="/admin/guestbook/delete/<%= m.id %>" style="display:inline;">
          <button>
            <%= lang === 'en' ? 'Delete' : 'Törlés' %>
          </button>
        </form>
      <% } %>

    </p>

  <% }) %>

<% } else { %>

  <p>
    <%= lang === 'en' ? 'No messages' : 'Nincs üzenet' %>
  </p>

<% } %>

</body>
